version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
    commands:
      - gradle -v
  build:
    commands:
      - gradle build

artifacts:
  files:
    - ./build/libs/exercise-manage-backend.jar